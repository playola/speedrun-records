import React from 'react';
import styled from 'styled-components';

const AppWrapper = styled.div`
  background-color: beige;
`;

const App = () => (
  <AppWrapper>
    <h1>Speedrun records app</h1>
  </AppWrapper>
);

export default App;
